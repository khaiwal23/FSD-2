Understand SPA architecture — explain how routing works without full page reloads.

Differentiate client-side vs server-side routing.

Configure routes using libraries like React Router.

Create multiple pages/views inside a SPA (Home, About, Contact, etc.).

Navigate between components using Link / NavLink.

Use dynamic routes & URL parameters.

Implement nested routing.

Handle 404 (Not Found) pages.

Pass data through routes.

Build clean, user-friendly navigation systems.git status
